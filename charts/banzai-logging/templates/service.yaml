apiVersion: v1
kind: Service
metadata:
  name: {{ template "banzai-logging.fullname" . }}
  labels:
    app: {{ template "banzai-logging.name" . }}
    chart: {{ template "banzai-logging.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "banzai-logging.name" . }}
    release: {{ .Release.Name }}
